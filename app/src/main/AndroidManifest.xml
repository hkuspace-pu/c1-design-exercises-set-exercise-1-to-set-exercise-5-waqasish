<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantManagementApplication"
        tools:targetApi="31">

        <!-- Launcher / Welcome Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Staff Login Screen -->
        <activity
            android:name=".StaffLoginActivity"
            android:exported="false" />

        <!-- Guest flows -->
        <activity android:name=".GuestLoginActivity" />
        <activity android:name=".GuestRegisterActivity" />
        <activity android:name=".GuestForgotPasswordActivity" />
        <activity android:name=".GuestHomeActivity" />

        <!-- Staff Dashboard (Reservations & Home) -->
        <activity
            android:name=".StaffHomeActivity"
            android:exported="false" />

        <!-- Food Menu Management Screen -->
        <activity
            android:name=".FoodMenuManagementActivity"
            android:exported="false" />

        <activity android:name=".ReservationManagementActivity" />

        <activity android:name=".SettingsActivity" />

    </application>

</manifest>